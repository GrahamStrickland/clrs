% Exercise 3.1-3

Since big-O notation gives an upper bound on the running time of a function,
it does not make sense to say ``at least" in reference to an O-class
of functions,
as ``at least" implies that we are referring to a lower bound.
