name: Build C++ Library with CMake and Test with CTest

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Release

jobs:
  build-test:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set reusable strings
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/cpp/build" >> "$GITHUB_OUTPUT"

    - name: Install Boost
      run: brew install boost

    - name: Configure CMake
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=clang++
        -S ${{ github.workspace }}/cpp/

    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }}

    - name: Test with CTest
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      run: ctest -C ${{env.BUILD_TYPE}} --output-on-failure

    - name: Configure CMake with sanitizers
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}-asan
        -DCMAKE_CXX_COMPILER=clang++
        -DENABLE_SANITIZERS=ON
        -S ${{ github.workspace }}/cpp/

    - name: Build with sanitizers
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }}-asan

    - name: Test with CTest under sanitizers
      working-directory: ${{ steps.strings.outputs.build-output-dir }}-asan
      run: ctest -C ${{env.BUILD_TYPE}} --output-on-failure
